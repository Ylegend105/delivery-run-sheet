// Shared Tailwind class strings so every input/select/button across the app
// looks and behaves the same (readable text, visible border, visible focus
// ring) instead of each form drifting slightly.

export const inputClass =
  "rounded-md border border-gray-300 bg-white px-2.5 py-1.5 text-sm text-gray-900 placeholder-gray-400 focus:border-blue-500 focus:outline-none focus:ring-2 focus:ring-blue-500 disabled:opacity-50";

export const primaryButtonClass =
  "rounded-md bg-blue-600 px-3 py-1.5 text-sm font-medium text-white hover:bg-blue-700 disabled:opacity-50";

export const cardClass = "rounded-lg border border-gray-200 bg-white";
